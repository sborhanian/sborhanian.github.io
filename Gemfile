source "https://rubygems.org"

# github-pages bundles Jekyll and every plugin enabled on GitHub Pages.
# Pin it to avoid version drift between local and production.
gem "github-pages", group: :jekyll_plugins

# webrick is required for Ruby 3+ (removed from stdlib).
gem "webrick", "~> 1.8"
